import { Container, Paper, Typography } from "@mui/material";

export default function AboutPage() {

    return (

        <Container component={Paper} style={{ height: 400 }}>
            <Typography variant="h2">
                Real Deals.. Find best movie deals!!
            </Typography>
        </Container>
    )
}